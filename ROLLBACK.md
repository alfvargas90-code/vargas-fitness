# Rollback if council fixes look wrong
To restore the pre-fix state:
  git reset --hard pre-council-fixes-2026-06-02
  git push -f origin main
Tag was pushed to origin/tags before the changes landed.
